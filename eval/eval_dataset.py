eval_questions = [
    "What does save_pretrained save to disk?",
    "How do I load a model in 8-bit quantization?",
    "What is the difference between LoRA and QLoRA?",
    "What parameters does LoraConfig take?",
    "How do I use the Trainer class for fine-tuning?",
    "What is gradient checkpointing and when should I use it?",
    "How do I apply a PEFT adapter to a base model?",
    "What does device_map=auto do?",
    "How do I merge a LoRA adapter back into the base model?",
    "What is the difference between from_pretrained and load_adapter?",
    "How do I enable Flash Attention 2?",
    "What is the purpose of the DataCollatorWithPadding?",
    "How do I push a model to the Hugging Face Hub?",
    "What does requires_grad=False do in the context of PEFT?",
    "How do I load a tokenizer for a specific model?",
    "What is the target_modules parameter in LoraConfig?",
    "How do I evaluate a model using the Trainer class?",
    "What is the difference between AutoModel and AutoModelForCausalLM?",
    "How do I save only the adapter weights with PEFT?",
    "What is r in LoraConfig?"
]

ground_truths = [
    "save_pretrained saves model weights as model.safetensors or pytorch_model.bin, the config as config.json, and for PEFT models saves adapter_model.safetensors and adapter_config.json instead of the full model.",
    "Pass load_in_8bit=True to from_pretrained, which requires the bitsandbytes library. Example: model = AutoModelForCausalLM.from_pretrained(model_id, load_in_8bit=True)",
    "LoRA fine-tunes in full precision while QLoRA combines LoRA with 4-bit quantization of the base model using bitsandbytes, significantly reducing memory requirements.",
    "LoraConfig takes parameters including r (rank), lora_alpha (scaling factor), target_modules (which layers to apply LoRA to), lora_dropout, bias, and task_type.",
    "Instantiate a Trainer with a model, TrainingArguments, train_dataset, eval_dataset, and tokenizer. Call trainer.train() to start fine-tuning.",
    "Gradient checkpointing trades compute for memory by recomputing activations during the backward pass instead of storing them. Use it when training large models that exceed GPU memory.",
    "Load a base model, create a LoraConfig, then call get_peft_model(model, peft_config) to wrap the base model with the adapter.",
    "device_map=auto automatically distributes model layers across available GPUs, CPUs, and disk to fit large models that exceed single GPU memory.",
    "Call model.merge_and_unload() on a PeftModel to merge adapter weights into the base model and return a standard model without adapter overhead.",
    "from_pretrained loads a full model or base model. load_adapter loads only a PEFT adapter onto an already loaded base model.",
    "Pass attn_implementation='flash_attention_2' to from_pretrained. Requires the flash-attn package and a compatible GPU. Model must be loaded in float16 or bfloat16.",
    "DataCollatorWithPadding dynamically pads tokenized inputs in a batch to the same length, using the tokenizer's padding token.",
    "Call model.push_to_hub('username/model-name') or use the Trainer with push_to_hub=True in TrainingArguments.",
    "requires_grad=False freezes a parameter so it is not updated during training. PEFT uses this to freeze base model weights and only train adapter parameters.",
    "Use AutoTokenizer.from_pretrained(model_name_or_path) to load the tokenizer associated with a specific model.",
    "target_modules specifies which layer types to apply LoRA to, such as query and value projection layers. Example: target_modules=['q_proj', 'v_proj'].",
    "Pass an eval_dataset to the Trainer and call trainer.evaluate() to run evaluation. Metrics are computed using the compute_metrics function if provided.",
    "AutoModel loads the base transformer without a task head. AutoModelForCausalLM adds a language modeling head for text generation tasks.",
    "Call peft_model.save_pretrained(path) on a PeftModel — it automatically saves only the adapter weights, not the full base model.",
    "r is the rank of the low-rank decomposition matrices in LoRA. Lower r means fewer trainable parameters and less memory; higher r means more capacity but more parameters."
]